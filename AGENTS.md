# AGENTS.md

本文件是仓库级代理协作入口，定位为全局规则与导航。具体模块实现规范以更近的 `AGENTS.md` 为准。

## 1. 作用范围

- 本文件作用于仓库根目录及其所有子目录。
- 若子目录存在更近的 `AGENTS.md`，必须优先遵循更近的文件。
- 涉及 `backend/` 的实现、测试、接口或重构时，先阅读 `backend/AGENTS.md`。

## 2. 项目定位

- 项目名称：`douyin-parser`。
- 目标：提供抖音视频/音频解析下载能力，并提供 AI 对话解析接口。
- 当前核心实现位于 `backend/`；根目录其他内容主要承担说明、计划和协作辅助职责。

## 3. 顶层导航

- `backend/`：Node.js 后端服务、AI 能力、测试和内置联调页面。
- `backend/AGENTS.md`：后端实现、接口、测试、错误处理、AI 分层的入口说明。
- `backend/docs/API.md`：后端 API 契约文档。
- `docs/`：项目级设计、调研和方案文档。
- `README.md`：项目入口说明。
- `.zcf/`、`.spec-workflow/`：过程性文档与计划，不作为运行时代码。
- `.serena/`：工具元数据，不作为业务实现目录。

## 4. 全局工作原则

- 遵循 `KISS`：优先直接、清晰、最小的实现。
- 遵循 `YAGNI`：只实现当前明确需要的能力，不预埋未来特性。
- 遵循 `DRY`：发现重复逻辑时优先在既有边界内复用。
- 遵循 `SOLID`：保持职责单一、依赖方向清晰。
- 非平凡编码任务开始前，先明确假设、边界、计划和验证方式；不要静默假设。

## 5. 全局协作约束

- 始终先读后写，先理解现有结构与边界，再修改代码。
- 非用户明确要求时，不主动执行 `git commit`、`git push`、建分支、改历史。
- 不修改真实凭据或敏感配置值，如 `backend/.env` 中的密钥。
- 不提交、不编辑 `node_modules/`、运行时缓存、临时产物或本地 IDE 配置。
- 不因“顺手”进行无关重构，改动范围应与当前任务直接相关。

## 6. 文档与注释

- 输出语言使用简体中文。
- 代码注释与文档优先保持简体中文，与现有仓库一致。
- 修改接口、运行方式、目录职责或协作规则时，同步更新相关文档。

## 7. 检索入口

- 后端实现、测试或重构：阅读 `backend/AGENTS.md`。
- API 契约：阅读 `backend/docs/API.md` 与 `docs/技术方案.md`。
- AI 能力：从 `backend/ai/`、`backend/routes/ai.js`、`backend/services/AIChatService.js` 开始。
- 启动、测试和依赖脚本：查看 `backend/package.json`，命令默认在 `backend/` 下执行。

## 8. 验证要求

- 文档-only 变更至少检查 Markdown 路径、标题层级和 `git diff --check`。
- 后端代码变更默认按 `backend/AGENTS.md` 的测试建议执行验证。
- 若无法执行应有验证，必须在结果中明确说明原因。

## 9. 输出期望

- 说明改动落点与原因，而不是罗列无关文件。
- 说明已执行的验证；未验证时明确写“未验证”。
- 若发现用户已有改动与当前任务冲突，应先暂停并确认。
